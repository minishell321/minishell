
HEREDOC:
After reading (https://linuxize.com/post/bash-heredoc/), the heredoc identifier (<<) cannot
be followed by leading whitespace characters (e.g. space), only tabs.
If the heredoc identifier (<<) is followed by a hyphen (-), the leading tab characters will
be ignored.
	
	=> Currently, the way we are tokenising the heredoc, the function does not check for
	the hyphen (-) and increments the string position while spaces are found. This needs
	to be addressed but I would like to verify the behaviour in bash to double-check.


Il y a des bugs (ex. le comportement de exit ou CTRL+C ou CTRL+D) que j'ai remarque apres avoir
lance plusieur commande de type builtin_cd. Il me semble que les bugs provienent non pas de la
fonction built_cd (car les memes bugs survienent quand j'appelle builtin_echo) mais plutot de la
maniere que l'on gere actuellement les pipes/forks lors d'un appelle d'une fonction builtin. J'ai vu
un commentaire dans le fichier exec_cmd indiquant que "le close pipe et non-systematique pour
l'instant". Je ne sais pas si c'est la cause du probleme mais c'est a addresser.


Remplacer tout les messages d'erreur en utilisant la fonction perror
